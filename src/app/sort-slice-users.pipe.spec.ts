import { SortSliceUsersPipe } from './sort-slice-users.pipe';

describe('SortSliceUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new SortSliceUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
